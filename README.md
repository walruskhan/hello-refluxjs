## Examples of refluxjs and react

* Note that triggerAsync() is different from async/await
* stores are automatically instantiated by react components that extend `Reflux.Component` and `Reflux.PureComponent`